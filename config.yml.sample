# A unique name of your service
service_name: feedify

# Default stage for the deployment, by convention it can be 'dev' for
# development and 'prod' for production, you can use the `--stage` option
# to override this in every command
stage: dev

# AWS region, see http://docs.aws.amazon.com/general/latest/gr/rande.html
# for all available regions
region: ap-northeast-1

# (optional) API key for Mercury Web Parser
mercury_api_key:

# (optional) Tracking ID of Google Analytics
ga_tracking_id:
